# Creator review: one clear path

### 1. See the phenomenon

Open `outputs/bz_research_twin.png`. The top panel is the well-mixed oscillator;
the lower panel is a spatial transient. Both are dimensionless and labeled as
proxies.

### 2. Review the synthesis path

Open `SYNTHESIS.md`. It separates what is already computed from what only a
creator or chemist can approve: a specific elementary step and mapped 3-D
endpoints.

### 3. Decide whether Rowan is worth the next step

Rowan becomes useful only after the molecular question is real. The proposed
sequence is endpoint validation → double-ended TS search → TS frequencies →
IRC/connectivity check → comparison with the macroscopic model. The creator
can reject the molecular interpretation before any credits are spent.

### 4. Optional data handoff

An anonymized CSV with `time` and an intensity/brightness proxy would allow a
period and waveform comparison. No recipe or private lab notebook is required.
